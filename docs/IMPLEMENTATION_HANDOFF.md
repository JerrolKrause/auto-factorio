# Implementation handoff

## Current implementation — Visible workshop startup and observability, 20 September 2026

The first blueprint-sandbox runs were accepted by HTTP but stopped with `workshop_runtime_failed:Error: actor_unavailable`. Production startup had created only the dedicated headless Factorio server, while Legal character construction requires the connected `builder-1` observer. Two failed sessions reached designer inference before that missing actor was discovered and recorded 18,983 plus 13,461 reported tokens. The journal retained raw provider and transition evidence, but the dashboard did not expose the terminal reason or a complete live operation timeline.

Ordinary `npm start` now launches a project-owned dedicated server plus visible Factorio client, waits for `builder-1`, then opens the dashboard. Headless operation is opt-in through `npm start -- --headless` or `AUTOFACTORIO_HEADLESS=1`; direct construction remains supported there. A supplied project server gets a matching visible observer unless headless was explicit, reuses an existing matching observer, and cleans only an observer this startup launched. Ownership is established at the successful Steam spawn boundary, so readiness failure or graceful interruption cannot leave that new observer behind. The startup RPC readiness loop tolerates Factorio's first console-command acknowledgement prompt.

Legal-character readiness is launch-only and runs before configuration or provider admission, so a missing builder rejects without consuming a model turn while character presets can still be saved offline. Workshop launch returns HTTP 202 to distinguish durable acceptance from completion. Preflight, provider, build and measurement activity is durably projected into the dashboard; terminal reasons remain inline and the selected evidence view tracks later state. Conclusive external failures persist `failed` and stop, while missing or ambiguous receipts persist `unknown` and hold for reconciliation. Provider prompts, credentials and hidden reasoning are not copied into public activity.

Final delegated verification is `.runtime/contracts/visible-default-verification-003/`: contract `valid`, `ready` and `sourceMatches` are true. `corepack.cmd pnpm verify` passed both builds, lint, **434/434 tests in 25 files** and docs; `corepack.cmd pnpm test:ui` passed **4/4** browser flows; `corepack.cmd pnpm verify --game` passed a fresh explicit-headless launch/smoke and exact cleanup; final `game:processes` returned `[]`. The author also ran ordinary `npm start`: profile `.runtime/phase03/game-cWoNdA` reported `visible: true`, `RCON and builder ready`, the dashboard reported a connected real session with two agents, no provider inference ran, Ctrl+C closed port 3000 and removed both project processes.

Independent review found **5 issues (1 P1, 4 P2); 5 fixed, 0 rejected, 0 remaining** across two corrective rounds. The final follow-up `.runtime/contracts/visible-default-review-003/` is valid, ready and source-matched with no findings. Decision record: [visible default and workshop observability](decisions/018-visible-default-and-workshop-observability.md). Baseline is `ca41f3054a809de27f76ca501e684fae31de168a` plus this uncommitted working-tree change; no commit, publish or new model-backed workshop run was performed.

Remaining limit: auto mode still opens the clearly labeled no-inference fixture if managed Codex or Factorio is unavailable; explicit `--real`, `--headless` or a supplied profile fails instead of silently falling back. **Next bounded action:** commit these fixes only on user instruction, or proceed with the user's next visible workshop run.

## Prior implementation — Startup and launch hardening, 20 September 2026

The user-facing entrypoint is now `npm start`. It installs the pinned lockfile dependencies through Corepack, builds the TypeScript/dashboard bundle, uses fixed `http://localhost:3000`, opens the browser, detects an existing AutoFactorio instance before starting duplicates, reports prerequisite/port/readiness failures in plain language, and cleans up an auto-created project Factorio server on interruption or partial startup. Default auto mode uses a real project Factorio + supported managed Codex session when both are discoverable; otherwise it explicitly starts a no-inference fixture mode. `--real`, `--fixture`, `AUTOFACTORIO_CODEX`, `AUTOFACTORIO_PROFILE_FILE` and `AUTOFACTORIO_FACTORIO_DIR` make the choice explicit. Real startup now waits for the AutoFactorio operator RPC after RCON becomes reachable, because the socket can open before the Factorio mod has registered its interface.

The dashboard now creates a same-origin HttpOnly/SameSite session cookie on the normal page boundary and publishes the stable localhost origin. Bookmarks no longer need a `#cap=...` fragment; bearer capability headers remain accepted for diagnostics and legacy descriptors. Health/readiness is explicit at `/health`. The low-level dashboard/scenario descriptors now provide a fragment-free URL plus the retained capability field.

Executed checks on the final relevant source: `corepack.cmd pnpm verify` passed build, dashboard build, lint, **424/424 tests in 24 files** and documentation at `.runtime/verification/check-bvEsuo/`; `corepack.cmd pnpm test:ui` passed **3/3** browser tests; both `npm start -- --fixture` and the default `npm start` reached `http://localhost:3000`, with the latter confirmed in connected Factorio mode. A second start reused the existing session. Cookie/API smoke passed and Ctrl+C left port 3000 closed with no Factorio process. `git diff --check`, `node scripts/check-docs.mjs` and assignment validation passed. No model inference was started for this change.

Remaining limits: Node.js and Corepack are prerequisites for invoking `npm` itself and are not silently installed; a missing/old Node version is reported with the required Node 24 LTS guidance. The automatic real path still depends on the user's licensed Factorio install and supported managed Codex executable; missing prerequisites intentionally result in an explicit fixture-mode notice unless `--real` or an explicit profile requests a hard failure. **Next bounded action:** review/adjudicate the fresh independent startup review, then preserve the returned contract and leave this worktree ready for the user's next instruction.

## Current implementation — Change-application reliability, 20 September 2026

The blueprint workshop remains complete at commit `970303fc9346d58da9966699b1b8b6e7b0a2fb3b`. Its direct follow-up hardening adds shared strict external-effect receipts, canonical cumulative learning bundles, one aggregate inference-admission owner, a production workshop composition root and the deterministic `change:ready` closeout gate. The tracked [readiness manifest](change-readiness/change-application-reliability.json) is **reviewed-clean** for `change-application-reliability`; detailed causes and corrections are in the [blueprint workshop retrospective](retrospectives/af-blueprint-workshop.md).

Final delegated verification `verification-c4c0ae2e-b9ed-4d37-9dea-3ec3e73ad30b` is valid, ready and source-matched. `corepack.cmd pnpm verify` passed both builds, lint, **424/424 tests in 24 files** and docs; `corepack.cmd pnpm test:ui` passed **3/3** flows; whitespace passed and `game:processes` returned `[]`. Independent review found **5 issues (1 P1, 4 P2); 5 fixed, 0 rejected, 0 remaining**. The final follow-up `review-903b4334-7cb8-4525-9b1f-80b43ac33718` is valid, ready and source-matched with no findings. No provider inference or Factorio launch was performed. Concurrent startup-path work was preserved and is outside this closeout.

**Next bounded action:** commit this hardening only on instruction. No archive, commit, publish or push is included in this task.

## Detailed blueprint workshop evidence — 19 September 2026

[`af-blueprint-workshop`](../openspec/changes/archive/2026-09-19-af-blueprint-workshop/tasks.md) is implementation-complete, synced and archived on baseline `162a9a601e695d38a610203d3c0b15a00034db4f` plus the source included in this closeout commit. All 27 tasks are complete. It adds versioned workshop/model/score/learning contracts, installed-data profiles, canonical native blueprint and immutable library storage, isolated workshop Lua privileges, direct and legal-character construction, exact-window evaluation/scoring, durable runtime-owned orchestration and usage ownership, dashboard/API flows, and exact-bundle learning activation with a confined helper worker. The trusted host resolves brief, preset, existing-revision and authorized full-contract launches against running installed data, supplies the exact compatible parent to Improve, and derives library verification and useful-distinction evidence inside the runtime rather than accepting labels from HTTP. The local library remains separate from deferred automatic external-library use.

Earlier delegated results remain retained at `.runtime/contracts/verification-assignment-1789850021086-6d1b8e79/`, `.runtime/contracts/verification-assignment-1789852279691-14fb6de5/` and `.runtime/contracts/verification-assignment-1789852882589-pauserecovery/`; they are historical snapshots after later corrective work. The first final-corrective verification at `.runtime/contracts/verification-assignment-1789856000000-final-corrective/` passed all eight assigned checks, both fresh game paths and source matching. Its audit then found six integration defects, all fixed. Final-remediation verification at `.runtime/contracts/verification-assignment-1789859000000-final-remediation/` subsequently passed eight checks with 56/56 source hashes stable. Review at `.runtime/contracts/review-assignment-1789860400000-final-remediation/` found five narrower defects, all fixed; source-pinned verification at `.runtime/contracts/verification-assignment-1789863000000-final-review-fixes/` passed all eight assigned checks and both fresh game paths. Review at `.runtime/contracts/review-assignment-1789864600000-final-review-fixes/` found four remaining issues, all fixed. Verification at `.runtime/contracts/verification-assignment-1789867600000-final-cancellation-learning/` passed T1-T8 with 57/57 source hashes stable, 408 tests, all browser flows, both fresh game paths and exact cleanup. Review at `.runtime/contracts/review-assignment-1789869000000-final-cancellation-learning/` found one void/malformed receipt issue, fixed. Verification at `.runtime/contracts/verification-assignment-1789870400000-explicit-receipts/` then passed T1-T8 on 411 tests and both fresh game paths with source matching and exact cleanup. Its review at `.runtime/contracts/review-assignment-1789871200000-explicit-receipts/` found two final P1 cancellation edges and one P2 status mismatch: gateway initialization was not yet registered for cancellation, receipt parsing still accepted malformed/contradictory positives, and completion documentation lagged the returned verification. All three are addressed: a session cancellation marker now fences setup before and after every pre-registration await, one strict parser rejects every malformed or contradictory receipt at host/runtime/provider boundaries, and this handoff records the exact prior evidence. The corrected source subsequently passed the final verification and review recorded below.

Final author game evidence uses project-only profiles with confirmed cleanup. The current direct path resolved the starter profile from running Factorio 2.0.77 prototypes and passed all eight direct/rollback checks at `.runtime/phase03/game-vvII1I/workshop-probe-Ldcyqr/`. The current isolated character path passed two comprehensive checks at `.runtime/phase03/game-macJGr/workshop-character-probe-akGNSn/`: a deliberately partial multi-item provision rolled every inserted delta back and then allowed the same operation identity to succeed, after which the workshop-owned character consumed exact finite inventory through legal batches. The retained measurement, Space Age, no-mod and 14-check pause/save/load recovery evidence remains source-relevant for unchanged mechanics.

The current production composition probe at `.runtime/phase03/game-4SulZd/workshop-live-composition-aKqdTW/` passed end to end with managed ChatGPT/Astra-low and a dedicated live game: catalog/auth preflight, installed-profile resolution, managed design, deterministic canonical adaptation, isolated direct materialization, an exact 60-tick window, managed library-blind scoring and explicit `no-valid-result` finalization. Its durable budget records exactly two workshop turns, two tools, 73,848 ms and 55,256 reported tokens across designer and scorer under the one session cap; cleanup is true and `game:processes` is empty. The deliberately tiny smoke does not claim a production-valid blueprint. Final source-pinned verification at `.runtime/contracts/verification-assignment-1789872600000-strict-cancellation/` passed T1-T8: **415/415 tests in 22 files**, build, dashboard build, lint, docs, all 3 browser flows, both fresh game paths, strict OpenSpec validation, retained managed-provider evidence checks and exact cleanup. Its contract is `valid`, `ready` and `sourceMatches: true`; no model/provider calls were made. The final source-matched review at `.runtime/contracts/review-assignment-1789873400000-strict-cancellation/` confirmed the setup fence and strict receipt normalization and returned one P2 status-only finding, resolved by this routine closeout under the documentation-maintenance exception. No actionable finding remains.

Corrected managed-provider evidence is `.runtime/phase17/provider-workshop-HpJXPy/report.json`. Codex CLI 0.155.1, managed ChatGPT authentication and Astra/low passed exact catalog/allowance preflight. The fixed manifest ran five exact unattended attempts, a separate acknowledged checkpoint/steer and a visible `NO_CHANGE`. The actual `LearningService` assembled a typed helper bundle, bound complete deterministic/interaction/behavior/review controls to hash `b3f8168fe6d2465ebafb99627f8971fc2e2cd93328725b87e96896cc60f7be10`, activated it by generation compare-and-swap, pinned a fresh provider session and invoked the confined helper. The fresh model called `observe` and consumed its runtime-supplied result `61`; the prompt did not contain the answer. The run used 9 turns and 136,678 reported cumulative tokens under 12-turn/250,000-token caps, with no fallback or Factorio mutation. Earlier provider runs remain retained as historical or failed trials.

The runtime now rechecks managed selections before each inference and admits all roles through one persisted aggregate budget. The configured learning turn/tool share also partitions elapsed provider time and finite reported tokens, and stable invocation identities prevent role-by-role reset. Unknown finite-token use closes that owner's later inference; any admitted maintenance failure records a durable deferred outcome without blocking completed blueprint delivery. External effects persist dispatched/acknowledged state; a replacement controller uses an adapter reconciliation result or holds the session rather than replaying an unknown provider/game/learning outcome. Stop and shutdown request cancellation before awaiting active work; provider and game adapters must return a confirmed completion or cancellation receipt, and a rejection or missing receipt marks dispatched intents unknown and leaves the session held. Learning records a typed no-change or proposal at after-session/batch cadence, requests optional human approval only for an already attested activation, and loads the exact cumulative pinned instructions and lessons into future sessions. Follow-on bundles retain incumbent files and required controls, while every nonempty bundle, including lesson-only updates, requires representative behavior evidence. A pinned design helper runs once through the confined worker; the trusted host admits only a revision-matched registered plan and retains consumption or fail-closed rejection evidence. Library score evidence stores the full canonical rubric plus its hash, preventing comparisons across reused labels with changed weights, directions or materiality.

The worker boundary combines a separate Node permission-restricted process with a capability-free VM context, immutable hash checks, empty environment, private scratch and finite wall/memory/input/output/invocation limits. This denies filesystem escape, child processes and exposed network/secret capabilities in tested helpers, but it is not a general-purpose OS network namespace. Unsupported rich mechanics remain rejected by the compatibility matrix rather than partially admitted. Phase 17 scenario retrospectives remain a separate future milestone.

Initial corrective review `.runtime/contracts/review-assignment-1789850594115-a5534c8d/` reported 8 findings (5 P1, 3 P2); all 8 were fixed. Follow-up review `.runtime/contracts/review-assignment-1789853403416-followup/` found 2 additional P1 issues; both were fixed. Review `.runtime/contracts/review-assignment-1789857200000-final-corrective/` confirmed those closures and found 6 further issues (4 P1, 2 P2); all six were fixed. Review `.runtime/contracts/review-assignment-1789860400000-final-remediation/` confirmed two of those fixes and found 5 narrower issues (4 P1, 1 P2); all five were fixed. Review `.runtime/contracts/review-assignment-1789864600000-final-review-fixes/` found 4 P1 issues; all four were fixed. Review `.runtime/contracts/review-assignment-1789869000000-final-cancellation-learning/` found 1 additional P1 issue; it was fixed. Review `.runtime/contracts/review-assignment-1789871200000-explicit-receipts/` found 3 issues (2 P1, 1 P2); all three were fixed. Final review `.runtime/contracts/review-assignment-1789873400000-strict-cancellation/` confirmed the two substantive closures and found 1 P2 status-only issue, fixed here. Across eight corrective reviews, all **30 unique findings (22 P1, 8 P2)** are fixed, 0 rejected and 0 remain. The final status edit is routine documentation maintenance; `node scripts/check-docs.mjs`, strict OpenSpec validation and `git diff --check` are the required author-run closeout checks.

## Blueprint workshop archive and commit closeout — 19 September 2026

Synced all three capabilities to [agent learning](../openspec/specs/agent-learning/spec.md), [blueprint library](../openspec/specs/blueprint-library/spec.md) and [blueprint workshop](../openspec/specs/blueprint-workshop/spec.md), then archived the complete change at `openspec/changes/archive/2026-09-19-af-blueprint-workshop/`, preserving `.openspec.yaml`. Commit: **`Implement and archive blueprint workshop`** (this closeout commit). Final archive checks cover exact delta-to-main comparison, strict main/archive OpenSpec validation, documentation links, whitespace and empty project game processes. The unrelated development-efficiency edits remain outside this commit.

**Next bounded action:** begin another milestone only on explicit instruction. No publish or push is included.

## Implementation handoff - Decision-focused context, 18 September 2026

[`af-decision-focused-context`](../openspec/changes/archive/2026-09-18-af-decision-focused-context/tasks.md) is implementation-complete, synced and archived. All 15 tasks are complete. The implementation adds versioned production targets and operational observations, ordinary Lua sampling, recipe-derived demand, explicit unsupported nominal capacity, scoped rates/stock, filtered snapshot inspection, compact actor/command views, role briefings, durable current-owner watches, delivery accounting and safe role-session reconstruction. Every mutation stays fenced until authorized pending effects and any required condition envelope reconcile; watch acknowledgements survive restart and scope revision without exposing restricted command IDs.

The no-inference production-path report is `.runtime/decision-context-comparison/report.json`. All four fact oracles pass. On the fixed 300-entity fixture, supply context changed from **54,201 bytes / 21 tool calls** to **2,460 bytes / 1 tool call**; idle-machine inspection is **3,346 bytes / 1 call**; the 61-step command changed from **7,251 bytes / 8 calls** to **250 bytes / 1 call**; replacement changed from **56,344 bytes / 22 calls** to **2,534 bytes / 1 call**. At 30/300/3,000 entities the scoped candidate remains 2,460 bytes with the same content hash while production pagination grows from 2 to 19 to 186 calls. These deterministic results establish fact coverage and serialized byte/call behavior only; token savings, reasoning quality and gameplay improvement remain unverified.

Final delegated verification is `.runtime/contracts/af-decision-context-verification-005/`: contract `valid`, `ready` and `sourceMatches` are true. Requested verifier route was `gpt-5.6-luna / medium`; observed model/usage metadata was unavailable. `corepack.cmd pnpm verify` passed both builds, lint, **367/367 tests in 19 files** and documentation (**154 Markdown files / 484 local links**) at `.runtime/verification/check-MNe69a/`. `corepack.cmd pnpm verify --game` passed the same software gate plus fresh headless launch/smoke and exact cleanup at `.runtime/verification/check-IFqWr9/`. The comparison passed, and final fresh S1 and S2 probes each passed **9/9 checks** with no model inference, held cleanup, pause behavior and managed save/load reconstruction: `.runtime/scenarios/runs/run-9Fn4VM/operational-probe-15rwOC/` and `.runtime/scenarios/runs/run-0wWTWo/operational-probe-TmaPZp/`. Their operator-only source independently checks raw production, consumption, public boundary counters and scoped inventory stock quantities/rates against sampler rows with a declared two-cadence tolerance; missing/null/incomplete raw-union stock coverage fails. Final `game:processes` returned `[]`.

Independent review rounds are retained at `.runtime/contracts/af-decision-context-review-001/` and `af-decision-context-review-followup-001` through `-003`. The initial review reported 9 findings (2 P1, 6 P2, 1 P3); follow-ups reported 3 P2 and then 2 P2 refinements. Fixes cover principal-authorized pending counts, all-operation mutation fencing, budget preservation, target/capacity semantics, durable overflow/revision watch delivery, current-owner routing, production-path comparison, independent flow/stock calibration and explicit empty-field rejection. The final follow-up is `valid`, `ready` and source-matched with no findings; no actionable issue remains.

Supported live measurements are normal-quality S1/S2 assembler production, recipe-supported item consumption, public terminal/collector delivery and scoped supported inventory stock, plus configured supply and recipe-derived target demand as separately labeled facts. Unsupported rich recipes/mechanics, arbitrary late-game machines, fluids, mining and theoretical machine capacity remain explicit unknown or outside initial coverage. Provider context occupancy and subscription balance also remain unknown when unavailable.

The bounded-context delta is merged into [`openspec/specs/bounded-context/spec.md`](../openspec/specs/bounded-context/spec.md), and the new capability is at [`openspec/specs/operational-observations/spec.md`](../openspec/specs/operational-observations/spec.md). Commit: **`Implement and archive decision-focused context`** (this closeout commit). No publish or provider/gameplay inference was performed during closeout.

**Next bounded action, only on instruction:** implement the blueprint workshop, begin Phase 14, or run a separately authorized model comparison. Do not start another milestone automatically.

## Planning handoff - Blueprint workshop, 18 September 2026

**19 September spec update:** Added session-default and per-role OpenAI model/reasoning-effort options, capability validation, pinned selection provenance/recovery and a separate provider identity for future expansion. Proposal/design/tasks are aligned; the packet now has **25 requirements / 41 scenarios**, with **27 tasks still unchecked**. Strict selected-change OpenSpec validation, documentation (**155 files / 490 local links**), task/routing coverage **27/27**, tracked/staged and before-snapshot whitespace checks passed. Fresh read-only review found **0 findings**, with `valid`, `ready` and `sourceMatches` true at `.runtime/blueprint-model-options/review/review-assignment-1789837239314-686ba8c5/`. This evidence-only handoff uses author-run checks. No implementation, game/provider probe or commit; runtime availability remains unverified. Next action remains workshop implementation only on instruction.

Created [`af-blueprint-workshop`](../openspec/changes/archive/2026-09-19-af-blueprint-workshop/proposal.md): proposal, design, three capability deltas (`blueprint-workshop`, `blueprint-library`, `agent-learning`), now **24 requirements / 38 scenarios**, and **27 unchecked implementation tasks** with model routing. Covers UI-configured unattended or human-assisted loops, technology variants, unlimited scoped sandbox supplies, direct/character construction, accelerated independent scoring, native exports, ordinary character reuse, inspectable histories and bounded autonomous learning. Planning only; no product code, main specs, game runs or gameplay inference changed. Original planning baseline was `faa798c55964e27aac5d35f2ed9a06b136f3a19f`; review corrections use HEAD `162a9a601e695d38a610203d3c0b15a00034db4f`. No commit was made for this planning work.

Initial independent review found no findings; its report remains at `.runtime/blueprint-workshop-review/review-assignment-1789772048357-19ad6509/`. A subsequent user-supplied spec review identified **1 high, 3 medium and 1 low/documentation finding**. All five were accepted and fixed: activated-helper confinement, isolated blind critique and derived-context provenance, exact combined-bundle validation with incumbent/generation checks, per-port every-window throughput/rounding/settling rules, and the implemented decision-context baseline. No findings were rejected or remain. Fresh read-only follow-up found **0 findings**; `.runtime/blueprint-workshop-fixes-review/review-assignment-1789781151857-1ca3be26/` validates `valid`, `ready` and `sourceMatches` true. Strict OpenSpec validation passed **21/21 items**, documentation **155 Markdown files / 490 local links**, task/routing coverage **27/27**, and new-file/tracked/staged whitespace checks. This routine handoff uses author-run checks. Runtime enforcement and measurement calibration remain future implementation acceptance, not claims from static review.

**Next bounded action, only on instruction:** apply the workshop change beginning with contracts/profiles, then follow its staged tasks and required live acceptance. Integrate with the implemented decision-context interfaces and their retained measurement limits; other scenario milestones are unchanged. Initial coverage limits, inactive outcomes for unvalidated learning and later REQUIREMENTS/Phase 17 reconciliation are explicit in the reviewed design.

## Planning handoff - Decision-focused context, 18 September 2026

Created [`af-decision-focused-context`](../openspec/changes/archive/2026-09-18-af-decision-focused-context/proposal.md): proposal, design, operational-observations and bounded-context deltas, and **15 unchecked implementation tasks** with model routing. The packet specifies deterministic operational answers, role briefings, targeted diagnostics, preserved actor/command facts, bounded notifications and safe session reconstruction. This paragraph records the original planning state; the implementation and archive status are in the current handoff above.

Strict OpenSpec validation passed **20/20 items**; documentation checks passed **147 Markdown files / 469 local links** after this handoff link, task/routing coverage passed **15/15**, and new-file plus tracked-diff whitespace checks passed. OpenSpec 1.13.1 was invoked through an ignored project-local npm cache because no CLI was on PATH; no global tool upgrade was made. Fresh read-only independent review found **0 findings**; `.runtime/decision-context-proposal-review/` contains the assignment/result, validated `valid`, `ready` and `sourceMatches` true. This status-only handoff uses author-run documentation checks; the substantive planning packet received independent review.

The user-requested additional `agent-graph` spec-reviewer pass found **1 P2 gap, medium confidence**: the declared metric key lacked stable scope identity. Fixed four sites across design, operational spec and tasks to distinguish `scopeId` from `scopeRevision` and verify independent histories/coverage for two scopes sharing a revision. Follow-up: **0 remaining findings**, all five reviewer checks passed, final contract valid/ready/source-matched at `.runtime/decision-context-spec-review-followup/`; the original finding is retained at `.runtime/decision-context-spec-review/`. Strict selected-change validation, portable artifact gate and documentation/whitespace checks passed. GitHub returned 404, so the reviewer used the clean local `agent-graph` copy at `5fe8c62fa232fa6cff88ba9d9040f6404ec62e5a`; its unavailable Opus pin was explicitly replaced with inherited author-model use. This was static planning review, not live measurement verification.

**Historical next action:** applying `af-decision-focused-context` was the next step at planning time; it is now complete and archived as recorded above.

## Current implementation — Phase 13 plate-to-science

Phase 13, [`af-13-plate-to-science`](../openspec/changes/archive/2026-09-18-af-13-plate-to-science/tasks.md), is implementation-complete, synced and archived. It adds the `02-some-assembly-required` S2 fixture, selector, reset path, installed-recipe stage graph, hidden legal reference and bypass controls. Tested source is commit `1754b4a` plus the Phase 13 source included in this closeout commit.

The frozen fixture is `s2-v6`: an 80×80 site, iron plates at 120/minute, copper plates at 60/minute, protected power/collector, and a finite normal-quality kit of 12 assembling machines, 300 belts, 60 inserters, 12 long-handed inserters, 14 underground belts, 6 splitters, 24 small poles and 8 wooden chests. Earlier solo/team launch evidence at `run-x0MTId` → `run-cnj5Wi` records the same physical fixture/checkpoint lineage and acknowledged reset-out barrier; evaluator-version changes intentionally give the final manifest a new fixture fingerprint. The two added underground belts distinguish S2 from S1's common kit.

`s2-measurement-v5` derives science and gear costs from installed Factorio 2.0.77 data. Its five-window minima are 300 terminal iron plates delivered to connected gear machines, 150 connected gears produced/delivered into science, 150 terminal copper plates delivered into science, and 150 machine-produced/collector-delivered science. Every stage must meet both total and observed-causal production minima. Complete category totals are paired with admission-stable stock components joined by active transport links. `s2-calibration-v5` preserves aggregate conservation residuals and sums component decreases, so ordinary movement within one active route is neutral while fresh accumulation behind an inactive boundary cannot cancel depletion of separate preloaded stock. It allows a four-gear upstream balance residual, 16 items of gear drawdown, zero residual for every other stage and 12 items of drawdown for other stages. Coordinate payloads accept bounded larger sites, but exact ownership still limits S1 to its default ±32 grant and S2 to an explicit ±40 grant.

### Live acceptance evidence

Final game-side and measurement-source evidence is `.runtime/phase04/game-nsrUM6/plate-to-science-probe-j3hDXf/`. The result has **7/7 checks passed**, no model inference and confirmed held cleanup at tick 93,145. The legal reference used **506 ordinary character-gateway steps**. Its five windows delivered **42, 42, 42, 42, 42** science. Stage produced/delivered/consumed/drawdown totals were: iron 545/546/546/1; gears 273/210/210/0 with -4/0 residuals; copper 300/210/210/0; science 210/210/210/0. Every stage passed. The later evaluator-only fix requires causal production at the same minimum; it is strictly more rejecting, the retained positive measurements satisfy every causal minimum, and the aggregate-cancellation gear stage records zero causal production. Focused tests and delegated verification re-evaluated that boundary without rerunning unchanged Factorio measurement code.

The same final run passed with **100 unchanged gears** in a topology-connected inactive side buffer; rejected preloaded/hand-supplied gears while science windows remained adequate; rejected a guarded character transfer; and invalidated native character inventory mutation. Its aggregate-cancellation control produced **258 gears**, delivered and consumed **210**, left **518 gears** across producer output chests, and passed every science window while a separate preload supplied the deficit; **210 items of active-component gear drawdown** rejected the attempt even though total fresh production exceeded delivery. Exact reports include `positive.json`, `unchanged-side-buffer.json`, `unchanged-side-buffer-stock.json`, `preloaded-gears.json`, `unused-upstream-shared-buffer.json`, `unused-gear-production.json`, `native-character-supply.json`, `sources.json` and `result.json` in that evidence directory.

Earlier failed calibration and probe runs are retained. They exposed and corrected S2 ownership/coordinate limits, a protected-fixture work position, installed recipe ordering, one unpowered iron inserter, the six-cell throughput shortfall, the 12-underground kit shortage, the four-item gear residual, oversized control mutations, heartbeat expiry, non-canonical targets and an ambiguous cumulative-production oracle. Independent review then found aggregate cancellation could hide an old-stock/unused-production bypass; a first admission-stock fix could reject unchanged buffers, and a later boolean path fix remained non-quantitative. Per-entity reconciliation closed that bypass but a follow-up review correctly found it could count ordinary stock movement as drawdown. The final admission-stable active-component reconciliation resolves that false-rejection risk while preserving inactive-branch detection. Fresh failed runs also retained an unknown heartbeat outcome, a legal 13-item drawdown against the old 12-item bound, and two control-wiring errors; no unknown outcome was retried in place, and every exact failed profile was held and stopped before a fresh run.

### Checks and next action

- Focused post-review checks passed build plus **45/45 tests** across `evaluation` and `first-shift`. `corepack.cmd pnpm verify` passed both builds, lint, **344/344 tests in 18 files** and documentation at `.runtime/verification/check-ezdt4S/`. Strict change/main-spec OpenSpec validation, documentation links and whitespace checks passed.
- Delegated verification at `.runtime/phase13-verification-causal-final/` passed all four criteria and prescribed checks with `valid`, `ready` and `sourceMatches` true. It requested `gpt-5.6-luna / medium`; observed model/usage metadata was unavailable. No provider/game inference was started.
- Final closeout review first found `[P1, high confidence]` causal production was measured but not enforced. The predicate and exact bypass regression were added; the finding is fixed. The source-matched follow-up at `.runtime/phase13-review-causal-final/` is ready with no findings. The immediately preceding review also found the per-entity false-drawdown P2; admission-stable active components fixed it. No actionable finding remains.
- The implementation used the recommended Sol/high route. Exact host model/usage metadata was not exposed to this session; `dev:usage` requires an explicit rollout directory/root and therefore no aggregate subscription balance or savings claim is inferred. No provider/model gameplay call ran.
- The reviewed delta was synced exactly to [`openspec/specs/plate-to-science/spec.md`](../openspec/specs/plate-to-science/spec.md). Archive/spec sync changed only reviewed planning placement and routine closeout status after the final source checks.
- **Next bounded action, only on instruction:** begin Phase 14, `af-14-ore-smelting-fuel`. Do not start it automatically.

## Phase 13 archive and commit closeout — 18 September 2026

All **8/8 tasks** are complete. The change is archived at `openspec/changes/archive/2026-09-18-af-13-plate-to-science/`; its four requirements are synced to the main `plate-to-science` capability spec. Commit: **`Implement and archive phase 13 plate-to-science`** (this closeout commit). The final game profile was safely held before process cleanup; personal saves, global settings, credentials, game binaries and unrelated Phase 03 processes were preserved.

## Prior implementation — 17 September 2026

The maintenance change [`af-development-efficiency`](../openspec/changes/archive/2026-09-17-af-development-efficiency/tasks.md) is implementation-complete, synced and archived. It adds five project-local commands: `dev:usage`, `dev:watch`, `dev:contract`, `dev:preflight` and preview-first `dev:task`; the supporting [workflow](DEVELOPMENT_EFFICIENCY.md) covers model roles, bounded delegation, checkpoints, resource serialization, repeated-failure diagnosis and compact handoffs. These helpers are advisory: they cannot hard-stop an interactive author, infer an exact subscription balance, authorize inference, or replace gameplay admission/verification.

Final post-fix evidence is `.runtime/verification/check-7or99l/`: both builds, lint, **337/337 tests in 18 files** and documentation passed. The no-inference composition manifest at `.runtime/preflight/check-1789659326550-8a41c264/` is ready for its declared fake-only scope and explicitly does not establish live-trial acceptance or authorize a game/provider run. Delegated verification at `.runtime/af-development-efficiency-verification/verification-assignment-1789658483312-e1e2bdaf/` covered all nine requirements. Independent review found **5 P2** defects; all five were fixed, none rejected or remain, and the source-matched follow-up at `.runtime/af-development-efficiency-review-followup/review-assignment-1789659360172-8bc8e5db/` is ready with no findings.

No live game, gameplay/provider inference, comparative model run or future routing pilot was started. Phase 12 remains archived with its unsuccessful S1 outcomes and incomplete scenario claims preserved. The next bounded product action remains Phase 13 only on explicit instruction; the separate real routing pilot belongs to that next authorized ordinary task, not this closeout.

Phases **01–12 are complete and archived**. Phase **12**, [First Shift agent trials](../openspec/changes/archive/2026-09-17-af-12-first-shift-agent-trials/tasks.md), has **8/8 tasks complete** and passed its bounded integration exit gate. Tested source is **`f4a41a251519a8254641d662405fdb69aa94e3d5` plus the Phase 12 source included in this closeout commit**. Its [capability spec](../openspec/specs/first-shift-agent-trials/spec.md) is synced to the main OpenSpec tree.

The [sanitized trial report](FIRST_SHIFT_AGENT_TRIAL_REPORT.md) is the primary result. It separates deterministic reference success, software/integration defects, operator interruption and bounded model outcomes. Neither gameplay trial reached S1 verification; Phase 12 permits complete evidence or explicit bounded model failure and does not require stochastic success.

**Next bounded action, only on instruction:** begin Phase 13 in a fresh conversation. Do not start it automatically. The first ordinary Phase 13 task may record the separately authorized routing pilot; it must not run a comparative benchmark.

## Development-efficiency archive and commit — 17 September 2026

Synced all nine requirements into [the main development-efficiency spec](../openspec/specs/development-efficiency/spec.md) and archived the complete change at `openspec/changes/archive/2026-09-17-af-development-efficiency/`, preserving its planning artifacts. Commit: **`Archive development efficiency tooling`** (this closeout commit). Archive checks cover strict main/change OpenSpec validation, exact delta-to-main comparison, documentation links and whitespace; no implementation behavior changed after final verification/review.

## Implemented behavior

The launcher composes isolated foreman/engineer managed-provider contexts, role-scoped MCP tools, legal character execution, durable coordination, independent S1 scoring and live dashboard telemetry. Its `s1-trials-v2` plan declares an unassisted team/fresh-engineer exercise and a separately assisted team exercise. Per run it allows 30 turns, 90 seconds and 20 attempted tools per turn, concurrency ceiling two with sequential scheduling, 30 game minutes, 45 wall minutes and 600,000 reported cumulative tokens. Telemetry closes admission after an admitted turn reports usage; it does not claim a hard pre-turn token cutoff.

The unassisted v2 evidence (`run-k1d7Mq` / `1c3e5d72-ae9d-43a2-91a6-acd315ea0583`) records separate engineer sessions, durable work pending at replacement binding, preserved budget, authorized reconstruction, a fresh world observation and one subsequent dispatch. The user-stopped run remains explicitly qualified: 6 admitted turns, last persisted total 504,701 tokens, unfinished sixth turn, no final report/evaluator and no proof of graceful shutdown.

The assisted exercise (`run-QPIx3a` / `ef13aee0-e262-4e61-8fe7-b75e6536f816`) completed as a bounded unsuccessful model outcome: 8 turns, 624,141 reported tokens, no ninth turn admitted, evaluator not requested, and confirmed interruption/cancellation with scoring closed. Developer-selected advice—“Check inserter directions and power before requesting verification.”—was delivered to the foreman at tick 8224, explicitly interpreted, linked to `s1-automation`, and forwarded to the engineer as resulting work. No causal improvement is claimed.

## Acceptance evidence

- Final pre-existing software gate: `corepack.cmd pnpm verify`, **309 tests in 17 files**, builds, lint and documentation passed at `.runtime/phase12-verification-5/result.json` and `.runtime/verification/check-2QnDPu/`.
- Current-source author gate: `.runtime/verification/check-Cj8XoI/` passed both builds, lint and **309/309 tests**; its documentation step correctly failed on two stale retrospective links. After removing them, `.runtime/verification/check-Nrp80w/` passed. Following final review fixes, `node scripts/verify.mjs` passed both builds, lint, **314/314 tests in 17 files** and documentation (**135 Markdown files / 446 local links**) at `.runtime/verification/check-opC9Ew/`. The preceding run `.runtime/verification/check-sdF76t/` retained one unrelated evaluation stress-test timeout; that file immediately passed **31/31** alone before the green integrated rerun.
- Headless game gate: `.runtime/phase12-verification-3/result.json` and `.runtime/verification/check-hVqu5i/`. Later changes do not alter its game/protocol boundary.
- Deterministic reference: **9/9 passed**, plus a **359,059-byte ledger** pause/recovery/20-heartbeat check at `.runtime/phase04/game-Xyoih3/first-shift-probe-A5yILP/` and `.runtime/phase12-reference-verification-3/`.
- Trial evidence verification: `.runtime/phase12-final-verification/` passed fresh-session, assistance and honest-outcome criteria. Its first exit interpretation incorrectly required evaluator success and remains retained. The follow-up `.runtime/phase12-exit-verification/` applied the literal design gate and validated ready with matching source hashes.
- Prior implementation review: eight rounds found **5 unique issues (1 P1, 4 P2)**, all fixed, under `.runtime/phase12-review-1/` through `phase12-review-8/`. Final closeout review at `.runtime/phase12-final-review/` found three additional P2 failure-boundary defects: preflight allowance refusal could exit successfully, replacement release was not bound to the held task/revision, and post-launch setup failure could bypass cleanup/reporting. Follow-ups at `.runtime/phase12-final-review-2/` and `phase12-final-review-3/` narrowed the cleanup defect, then found one additional P2: its hold could bypass the shared serialized RCON connection while polling remained active. The final source owns each acquired resource, closes it once in reverse order, records outcomes, and routes failure holding through the shared serialized lifecycle; focused regressions cover partial setup and an in-flight poll. All **9 unique findings (1 P1, 8 P2)** are fixed, none were rejected or remain, and `.runtime/phase12-final-review-4/result.json` validates ready with matching source hashes and no findings.

## Retained failures and limits

The report retains the zero-turn empty-rollout integration failure, reset/readiness failure, seven-turn software abort, interrupted corrected unassisted run, no-inference preflights and assisted token-cap result. No API billing fallback, credit purchase, model substitution or unbounded retry occurred. Full provider transcripts, credentials, saves and game data remain ignored.

Browser screenshot inspection was unavailable during the trials. Authenticated dashboard/API and provider-event evidence was retained, so role activity and control state are inspectable, but visual presentation remains unverified. The assisted run's project game is held with confirmed cancellation; the existing unrelated Phase 03 server was preserved. Personal saves, global settings and game binaries were not changed.

Phase 12 does not claim that Astra completed S1, that assistance improved performance, or that reported tokens convert exactly to subscription allowance. Scenarios S2–S5 remain planned.

## Archive and commit closeout — 17 September 2026

Synced all four `first-shift-agent-trials` requirements to the main spec and archived the complete change at `openspec/changes/archive/2026-09-17-af-12-first-shift-agent-trials/`, preserving `.openspec.yaml`. Commit: **`Implement and archive phase 12 agent trials`** (this closeout commit).

This closeout changes only status, links and faithful spec placement after the reviewed implementation. The **314-test** software gate, headless game gate, **9/9** reference gate, trial-evidence verification and final finding-free review remain applicable. Final archive checks cover strict main/change OpenSpec validation, documentation links and whitespace. No Phase 13 work or push is included.
