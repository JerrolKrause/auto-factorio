# Initial rosters

[team and soloTeam](../../packages/core/orchestration/roles.ts) share registration, messaging, task and budget contracts. Team uses a bodyless foreman plus one engineer authorized for `builder-1`; solo combines their tool groups in one identity. The roster budget defaults to at most two concurrent turns through the existing budget caps. Adding a specialist registers a definition and instance; domain-specific tools remain separate work.
